before using this make sure u have the following tools
-figlet socat
-ytp nvm im too lazy heres a local ai written one # lyric-tui

A lightweight, centered Terminal User Interface (TUI) that fetches and displays big, bold ASCII lyrics in real-time sync with your music. Built specifically for Termux and Linux environments.

## Dependencies

Make sure your system has the required backend tools installed:

```bash
pacman -S mpv figlet socat jq yt-dlp curl
or apt install mpv figlet socat jq ytp-dlp curl
